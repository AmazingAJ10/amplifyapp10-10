type SubscriptionApp @model @auth(rules: [{allow: public}]) {
  id: ID!
  SubscriptionName: String
  NextPayment: String
  Cost: String
  Logo: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
  author: String
}
 